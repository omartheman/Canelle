
//This script is for adding the right header font to each page.
const headerFont = '<link href="https://fonts.googleapis.com/css?family=Cinzel:700&display=swap" rel="stylesheet">'
document.querySelector('title').insertAdjacentHTML('beforebegin', headerFont);